path = "{{ path }}"
data = "{{ data }}"
data.custom = "{{ data.custom }}"
contents = "{{ contents }}"
base = "{{ base }}"
cwd = "{{ cwd }}"
other = "{{ other }}"
assigned = "{{ assigned }}"
props = "{{ props }}"
file = "{{ file }}"
file.path = "{{ file.path }}"
file.isBuffer = "{{ file.isBuffer() }}"
file.isStream = "{{ file.isStream() }}"
